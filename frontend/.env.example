# Frontend environment variables (example)

# API and WebSocket base (behind frontend Nginx proxy use relative paths)
VITE_API_BASE=/api
VITE_WS_BASE=/ws

# OAuth (used for login button / client-side config)
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

